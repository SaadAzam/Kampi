import { describe, expect, it } from 'vitest';

describe('api health placeholder', () => {
  it('passes smoke placeholder until integration harness is wired', () => {
    expect(true).toBe(true);
  });
});
